{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "gcc stacks.c checker.c libft/libft.a -o push_swap -g",
            "options": {
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}